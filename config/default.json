{
  "port": "env(PORT)",
  "settings": {
    "bodyParser": {
      "limit": "50mb"
    },
    "loggerFormat": "tiny",
    "session": {
      "store": "@foal/typeorm",
      "csrf": {
        "enabled": true
      }
    },
    "jwt": {
      
    }
  },
  "database": {
    "type": "mysql",
    "host": "localhost",
    "port": 3306,
    "username": "root",
    "password": "",
    "database": "app-yitodev"
  }
}
